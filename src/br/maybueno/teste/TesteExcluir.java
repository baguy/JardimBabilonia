package br.maybueno.teste;

import br.maybueno.model.PlantaModel;
import br.maybueno.model.JardimModel;
import javax.persistence.EntityManager;
import javax.persistence.EntityManagerFactory;
import javax.persistence.Persistence;

/**
 *
 * @author Baguy
 */
public class TesteExcluir {
    
        public static void main(String[] args){
    
      /*
        EntityManagerFactory emf = Persistence.createEntityManagerFactory("BabiloniaPersistencia");
        EntityManager em = emf.createEntityManager();
        
        JardimModel j = new JardimModel();
        PlantaModel p = em.find((PlantaModel.class), 1);
        //int index = j.getPlantas().indexOf(p);
        //j.removerPlanta(index);
            
        //PlantaModel p = em.find(PlantaModel.class, 2);
        
        // EXCLUIR FILHA SEM EXCLUIR PAI -- NÃO FUNCIONANDO
        //JardimModel j = em.find((JardimModel.class), 6);
        //j.getPlantas().remove(0);
                
                
         
        em.getTransaction().begin();
        em.remove(p);
        em.getTransaction().commit();
        em.close();
        emf.close();
*/

        
        
        }
    
}
